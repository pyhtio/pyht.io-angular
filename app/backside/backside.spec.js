"use strict";

describe("myApp.backside module", function () {
  beforeEach(module("myApp.backside"));

  describe("backside controller", function () {
    it("should ....", inject(function ($controller) {
      //spec body
      var backsideCtrl = $controller("backsideCtrl");
      expect(backsideCtrl).toBeDefined();
    }));
  });
});
